import * as THREE from 'three';
export const textureLoader = new THREE.TextureLoader();

/**
 * Sizes
 */
export const sizes = {
  width: window.innerWidth,
  height: window.innerHeight,
};
console.log('utils.js已加載');
